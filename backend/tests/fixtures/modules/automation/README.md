# Automation (ZIP package)

Triggers via Platform `events.declare` / EventCatalog discovery.
Delay → `$ctx->scheduler()->registerHandler('resume')` → `automation.resume`.
`update_submission` is FormsSubmissionCompatAction (tech debt).
Authoring SoT: `modules-src/automation/`.
