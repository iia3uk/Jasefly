# SDK Boundary Probe

Synthetic architectural fixture — not a product module.

Proves an **unknown ZIP slug** can install/load via Module Package Manager + Platform SDK
without adding that slug to any Jasefly Core map/switch/import.

Surfaces covered: route, permission, event publish/subscribe, admin page (own Component),
builder widget with `stableType`, enable/disable.
