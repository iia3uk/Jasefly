/**
 * Automation FE moved to installable package: modules-src/automation/frontend-dist.
 * Host loads it via packageModuleLoader when the package is enabled.
 * Admin UI remains host-bound via provideHostAdminPage (automation.admin).
 */
export {}
