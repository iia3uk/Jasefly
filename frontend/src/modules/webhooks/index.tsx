/**
 * Webhooks FE moved to installable package: modules-src/webhooks/frontend-dist.
 * Host loads it via packageModuleLoader when the package is enabled.
 *
 * This file remains so existing deep imports do not break; it does not register UI.
 */
export {}
