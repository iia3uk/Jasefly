/**
 * @deprecated Beacon lives in the analytics ZIP package and mounts via
 * `ctx.host.registerSlot('site.body.end', …)`. SiteLayout uses `<HostSlot />`.
 */
export function AnalyticsBeacon(): null {
  return null
}
