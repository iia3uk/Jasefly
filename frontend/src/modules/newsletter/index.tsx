/**
 * Newsletter FE moved to installable package: modules-src/newsletter/frontend-dist.
 * Host loads it via packageModuleLoader when the package is enabled.
 * Admin UI remains host-bound via provideHostAdminPage
 * (newsletter.subscribers / newsletter.campaigns).
 */
export {}
