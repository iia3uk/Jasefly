/**
 * Forms FE moved to installable package: modules-src/forms/frontend-dist.
 * Host loads it via packageModuleLoader when the package is enabled.
 * Admin UI remains host-bound via provideHostAdminPage (forms.admin / forms.submissions).
 */
export {}
