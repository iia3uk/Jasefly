/**
 * @deprecated Implementation lives in modules-src/comments (package FE).
 * Frozen widget IDs remain: comments, reviews, rating-summary, review-form.
 * Host no longer statically registers these — package uses Platform stableType.
 */
export function registerCommentWidgets(): void {
  // no-op: kept so accidental imports do not throw
}
