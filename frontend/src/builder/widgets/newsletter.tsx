/**
 * @deprecated Implementation lives in modules-src/newsletter (package FE).
 * Frozen widget ID remains: newsletter-signup.
 * Host no longer statically registers this — package uses Platform stableType.
 */
export function registerNewsletterWidgets(): void {
  // no-op: kept so accidental imports do not throw
}
