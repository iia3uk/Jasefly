/**
 * @deprecated Implementation lives in modules-src/forms (package FE).
 * Frozen widget ID remains: form.
 * Host no longer statically registers this — package uses Platform stableType.
 */
export function registerFormWidgets(): void {
  // no-op: kept so accidental imports do not throw
}
