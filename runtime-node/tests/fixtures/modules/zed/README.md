# Zed — synthetic unknown package probe

Not a product feature. Proves Node package host can install/enable/run an unknown ZIP without source whitelist changes.

Build ZIP: see `package-host-zed.test.ts` (or `node scripts` in test).
